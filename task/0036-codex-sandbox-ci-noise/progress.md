# Progress: `0036-codex-sandbox-ci-noise`

- **Target Spec:** `task/0036-codex-sandbox-ci-noise/spec.md`
- **Branch:** `feat/0036-codex-sandbox-ci-noise`
- **PR:** `未作成`
- **Status:** `Blocked` (Phase: `Verify (外部)`)
- **Complexity:** `S`

## タスクチェックリスト

構文チェックとテスト実行はここに書かない。`npm run ci` が強制する。

- [x] Specの要件・受け入れ条件の確認
- [x] 実装 (`.claude/agents/codex-reviewer.md` への規約追記)
- [x] 完了条件 5・6 の grep 確認（出力を会話に貼る）
- [x] 完了条件 7 の再現手順（実測 CI 結果あり／なしのレビュー依頼各 1 回）
- [/] レビューサブエージェント (`codex-reviewer`) の承認取得
- [ ] PR作成（進捗の **PR** に URL を書く。見た目の変更なら該当箇所のスクリーンキャプチャを本文に添付する。リポジトリには置かない）
- [ ] PRマージ後のアーカイブ

## 試行ログ・エラー履歴

- `04:04` - backlog から `task/` へ昇格。対処 (a)（`.claude/agents/codex-reviewer.md` への規約追記）を採ると親が決定し、完了条件を確定した。0026 のアーカイブ済みにより依存は解消済み。
- `06:07` - `## テスト結果の扱い` 節を `## 手順` の直後に追加した（8 行追加・既存行の削除変更ゼロ）。実装は Complexity S に従い haiku へ委譲。
- `06:07` - 完了条件 5・6 を grep で確認。5 は 24 行目、6 は 26・28 行目に該当行を確認した。`git diff --name-only` は `.claude/agents/codex-reviewer.md` のみで、範囲外（`tests/` 等）に変更なし。
- `06:07` - `npm run ci` 実行。401 件 pass / 0 fail。
- `06:22` - Verify (外部) 1 回目（実測 CI 結果あり）。**承認**。Critical 0 / High 0（Medium 2 / Low 1）。codex 本体は P1 を 1 件出したが、レビュアーが Medium へ降格した。理由は (1) この run で codex が実行した 6 コマンドにテスト実行が無く `EPERM` / `mkdtemp` の出力もゼロで P1 の前提が実測に支持されない、(2) codex の提案（`codex review` に `[PROMPT]` を渡す）は spec の「既存の手順は変えない」に反し実装すれば範囲外（Critical）になる。例 3 行目は再現できた（釈明を含まないレビューが返った）。
- `06:22` - Verify (外部) 2 回目（実測 CI 結果を添付せず＝例 4 行目の再現）。**不承認。High 2 件。** 例 4 行目は再現した（承認されず実測結果の提示を求められた）。ただし同時に、追記した規約が `CLAUDE.md` の「トークンコスト」節と衝突することが判明した。
- `06:22` - **High 1（設計上の衝突）**: `CLAUDE.md`「レビューサブエージェントへ渡すのは**差分と対象 spec だけ**」に対し、追記した 28 行目は「実測 CI 結果が無ければ承認しない」を要求する。規則どおりに依頼すると常に不承認になる経路ができる。今回の 2 回目の依頼がその実例である。
- `06:22` - **High 2（対処 (a) の到達範囲）**: 追記した規約は外側のレビューエージェントしか拘束せず、`codex review --uncommitted` で起動する codex プロセスには届かない。ただし 1 回目・2 回目とも codex はテストを回しておらず、再現はしていない。spec が「既存の手順は変えない」としているため、codex 側にプロンプトを渡す修正はこの作業の範囲外である。
- `06:22` - 両 High とも spec 側または `CLAUDE.md` 側の判断を要し、実装エージェントの独断では直せない。`CLAUDE.md` のルール変更は独立した docs PR の対象であり、この作業ブランチに混ぜられない。**Status を `Blocked` にし、人間の判断を待つ。** レビュー往復は 2 回で上限 5 回には達していない。
- `07:26` - **High 1 の対処方針が決まった。** `CLAUDE.md`「トークンコスト」節を「渡すのは差分・対象 spec・実測の CI 結果だけ」に改め、「レビュアーはサンドボックス内でテストを再実行しない」を明記する案（(a)）を人間が選択した。規約どおり独立した docs PR にした: https://github.com/t2421/simple-loop-engineering/pull/62 （全 5 チェック pass・マージ待ち）。codex は起動時に `CLAUDE.md` を読むため、この改訂は High 2 も同時に閉じる。**この作業はその PR のマージまで Blocked。** マージ後に再レビューへ出す。
