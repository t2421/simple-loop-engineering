# Progress: `0044-second-project-port`

- **Target Spec:** `task/0044-second-project-port/spec.md`
- **Branch:** `feat/0044-second-project-port`
- **PR:** `未作成`
- **Status:** `In Progress` (Phase: `Verify (外部)`)
- **Complexity:** `L`

## タスクチェックリスト

構文チェックとテスト実行はここに書かない。`npm run ci` が強制する。

- [x] Specの要件・受け入れ条件の確認
- [x] 移植先 P の現状の把握（既存の `CLAUDE.md`・`.claude/`・検証コマンドの所在・テストの実体）
- [x] `.claude/skills/loop-port` のカタログ 13 グループを、移植先 P の対応物へ 1 件ずつ対応づける
- [x] 移植先 P へ手で移植する（共通化・自動化はしない）
- [x] ゲートが**落ちること**の実測 3 件（凍結ガード / 進捗結合 / Stop hook）。省略したものは理由を記録する
- [x] 記録の作成 (`task/0044-second-project-port/notes/port-log.md`)
- [x] 匿名化の確認（移植先 P を特定できる識別子が作業ディレクトリに 0 件。→ 完了条件 7）
- [x] このリポジトリ側に差分が無いことの確認（→ 完了条件 8）
- [/] レビューサブエージェント (`codex-reviewer`) の承認取得
- [ ] PR作成（進捗の **PR** に URL を書く）
- [ ] PRマージ後のアーカイブ

## 試行ログ・エラー履歴

- `17:59` - backlog から昇格。移植先を確定した。当初想定の「Swift か Python」ではなく、**パッケージマネージャを持たない Bash 主体のリポジトリ**（別組織 private）。`npm ci` の対応物が無く、検証コマンドの定義がワークフロー YAML 直書きであるため、0042 が必須項目に置こうとしていた `install` と、`check-protected-paths.mjs` の `definedIn` 設計を最初の 1 件で反証しうる。
- `17:59` - このリポジトリは public、移植先は private であるため、spec・progress・記録に移植先の識別子を書かない方針を完了条件 7 に固定した。
- `18:17` - 移植を完了し `notes/port-log.md` を書いた。移植先 P の worktree 上のローカルブランチで作業し、**push もマージもしていない**。
- `18:17` - 予測照合の結果: 4 分類のうち **当たったのは「真に固有」の 1 つだけ**。最悪の外れは `check-progress-coupling.mjs` で、無変更でコピーすると**実行でき、exit 0 を返し、あらゆる差分を対象外にする**。固有語スキャンの検索語にレイアウト語（`src/` 等）が無かったことが原因。
- `18:17` - 最大の発見 3 件。(a) P の台帳が `.gitignore` で除外されており、差分ベースのゲートが原理的に成立しなかった。(b) P には既に凍結があり（YAML の中身をテストで固定）、移植元の「1 ファイルに集約して凍結する」設計が正面から破壊した（`Tests: 24, Failures: 1`）。(c) P の Phase 0 ゲートと `start-task` の入口が二重になる。
- `18:17` - ゲート 3 件（凍結ガード / 進捗結合 / Stop hook）はすべて「落ちること」を実測できた。省略したゲートは無し。
- `18:17` - 匿名化を確認。組織名・リポジトリ名・固有プロダクト名を並べた `grep -rniE` が 0 件（検索語そのものはここに書かない。書けば記録が識別子を含むことになる）。
- `18:45` - Verify (外部) 1 回目。`codex-reviewer` は **不承認**。Critical 0 / High 2 / Medium 6 / Low 1。
- `18:45` - High 1: `guard-worktree.mjs` を「無変更で動いた」と記録していたが誤り。定数が `['src','tests','tools']` のままでは移植先の実装編集をブロックせず、`PreToolUse` への登録も無かった。**`check-progress-coupling` と同型の取りこぼしに 2 度落ちていた。** 移植先で適合させ、宣言した実装パスが 1 つ残らずブロックされることを実測して記録に貼った。
- `18:45` - High 2: 記録の置き場（作業ディレクトリ直下の `port-log.md`）が、このリポジトリの「別名 spec 禁止」規則に触れ、`protected-paths` が exit 1 で落ちる。**昇格時に私が spec で指定した置き場が誤っていた。** CLAUDE.md「凍結を解いて改訂するとき」に従い、改訂内容と「検証を弱めない理由」を spec に書いたうえで `notes/port-log.md` へ移した。**この PR には人間による `allow-protected-change` ラベルが要る。**
- `18:45` - Medium 6 件も反映。とくに Stop hook の実測中に、**移植元の Stop hook が検証失敗時に exit 1（非ブロッキング）を返す**穴を見つけた。止めるのは exit 2 で、`check-actions.mjs` は 2 を使っている。移植先では 2 に直して実測した。移植元の修正はこの作業の範囲外なので、backlog 起票を申し送りにした。
- `18:45` - 修正後の実測: `npm run ci` は 466 pass / 0 fail。`protected-paths` はラベル無しで exit 1（spec の凍結改訂を検知）、ラベル付きで exit 0。`progress-coupling` は docs のみのため対象外で exit 0。
- `19:05` - Verify (外部) 2 回目。**不承認**（通算 2 回目、上限 5）。1 回目の High 2 件は解消と判定された。新規 High 1 件: **`CLAUDE.md` を手移植しておらず、spec の「例」が要求する「書き換えた記述の一覧」が空**。
- `19:05` - 完了条件を緩める道（spec の「例」から `CLAUDE.md` 移植を外す）は取らず、**実際に移植した**。移植先の `CLAUDE.md` に 4 節を追加し既存 1 節へ追記（`+56 行`）。書き換え不要だった記述 8 件・書き換えた記述 6 件を記録に列挙した。
- `19:05` - 移植して分かったこと 2 点。(1) 最も価値が高かったのは「報告の作法」で、移植先には出力を貼る規定が無く QG 判定が根拠を伴わないまま書かれうる状態だった。(2)「Critical ゼロ」は原則としては不変だが、移植先には既に同じ構造があり、節を新設せず既存節へ 1 行足すのが正しかった。**「不変の原則だからそのまま移す」では誤る。**
- `19:05` - Medium 1 件（申し送りの「3 項目」と表の 4 件の食い違い）を 4 項目に訂正。Low 3 件（progress と spec の旧パス参照、未追跡ファイルの残差）も反映。
- `19:35` - Verify (外部) 3 回目。**不承認**（通算 3 回目、上限 5）。2 回目の High（`CLAUDE.md`）は解消と判定。新規 High 2 件は**どちらも同じ型**——「移植先の既存運用との衝突」の取りこぼし。2 回続けて同じ型を落としている。原因は 2.6 を「発見 3 件」と閉じた形で書いたことにある。
- `19:35` - High (d) 台帳のライフサイクル不一致。移植先は台帳を実装ブランチで新規作成するため、**通常の実装 PR がすべて進捗結合で落ちる**。実測で再現した。解き方 2 つのうち「新規作成も数えるようチェッカーを緩める」は**取らない**。移植先で落ちるからゲートを緩めるのは、このループが最も禁じている操作そのものである。未解決として記録した。
- `19:35` - High (e) 「別名 spec 禁止」が移植先の台帳 5 文書構成と衝突。単数の除外を**許可リスト（複数）**に変えて解いた。許可リストに無い名前は従来どおり弾かれることを実測した。
- `19:35` - Medium 2 件も移植側で修正。`GATE_HELPERS` に `guard-worktree.mjs` と hook の配線（`settings.json`）を追加した（**判定コードだけ守っても、呼び出し側を落とせばゲートは呼ばれない**）。残っていた旧語彙 3 経路を潰した。
- `19:35` - 「文言は追随しない」という項目を記録に書いた**後もなお 2 回踏んだ**。人手で潰す限り漏れるので、機械化（利用者向け文字列リテラルへの禁止語走査）を 0043 への申し送りに入れた。
- `19:35` - Low: レビュアーの生ログには移植先の絶対パスが入る。**ファイル側が匿名でも会話経路で破れる**ため、貼るのは判定ブロックだけにする旨を記録の 6 節に残した。
