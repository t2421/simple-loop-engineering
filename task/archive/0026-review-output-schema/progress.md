# Progress: `0026-review-output-schema`

- **Target Spec:** `task/archive/0026-review-output-schema/spec.md`
- **Branch:** `feat/0026-review-output-schema`
- **PR:** https://github.com/t2421/simple-loop-engineering/pull/57
- **Status:** `Done`
- **Complexity:** M

## タスクチェックリスト

構文チェックとテスト実行はここに書かない。`npm run ci` が強制する。

- [x] Specの要件・受け入れ条件の確認
- [x] `.claude/agents/codex-reviewer.md` に出力スキーマ・Critical 列挙・テスト提案規則・スキーマ違反時の扱いを追記
- [x] `.claude/agents/visual-design-reviewer.md` に出力スキーマ・Critical 列挙・テスト提案規則・スキーマ違反時の扱いを追記
- [x] 完了条件 5〜8 の `grep` 検証（出力を会話に貼る）
- [x] レビューサブエージェント (`codex-reviewer`) の承認取得
- [x] PR作成（進捗の **PR** に URL を書く）
- [x] PRマージ後のアーカイブ

## 試行ログ・エラー履歴

- 2026-08-22 - backlog から昇格。2026-08-22 の実測（レビュー 6 回で subagent トークン約 60 万、うち 2 回で出力が無害化により変形）を受けて着手を決定。スキーマ範囲・Critical 列挙粒度・テスト提案規則の 3 点は人間が判断済み（spec の背景に記載）。
- `04:20` - 両定義に同じ構造で 3 節を追記した。**出力スキーマ**（severity / 根拠のパスと行 / 完了条件番号 / 一文の要約 の 4 項目を必須、長文は任意の補足欄へ、4 項目を欠くものは指摘として数えない）、**Critical の条件**（列挙し、列挙外は High 以下）、**テストで書ける指摘**（決定論チェックで真偽が決まる指摘はテスト追加の提案として返す）。承認節には「出力スキーマに合わない結果は承認として扱わない」を足した。既存の承認条件・severity 対応づけ・5 回上限・Blocked への遷移は 1 文字も変えていない。
- `04:20` - Critical の列挙は spec の指示どおり。`codex-reviewer`: 期待値改ざん / 範囲外の実装 / P0 相当の欠陥。`visual-design-reviewer`: トークン不一致 / 構造の欠落 / 抽出物の書き換え。どちらも列挙外の例（前者は「読みにくい・命名の好み」、後者は「整列・重なり・階層の残差」）を明示して、境界が読み手によって揺れないようにした。
- `04:22` - 完了条件 5〜8 を `grep` で実測。`severity`・`完了条件番号`・`一文の要約`・`High 以下`・`テスト追加の提案`・`承認として扱わない` の 6 語すべてが**両ファイル**にヒットした。「例」の `grep -c '列挙' codex-reviewer.md` は `2`（1 以上）。出力は会話に貼った。
- `04:25` - 完了条件 9・10 を実測。`npm run ci` は fail 0。`git diff main --stat` は `.claude/agents/` の 2 ファイル（+82 行）だけで、`src/`・`tools/`・`.github/` に差分は無い。
- `05:10` - **Verify (外部) 1 回目: 不承認。Critical 0 件・High 2 件・Medium 3 件・Low 2 件。** レビュアーが新スキーマの形式で返してきたので、書式そのものは運用できている。指摘は全件直した。
- `05:20` - **H1（最も重い）。** 「決定論チェックで真偽が決まる指摘はテスト追加の提案として返す」に優先順位が無く、Critical / High を承認ゲートの外へ逃がす抜け穴になっていた。このリポジトリの Critical 列挙 3 件はいずれも機械判定できる側にある（期待値改ざんは `check-protected-paths.mjs` が、常に通る経路はテストが判定できる）ため、実際に成立する穴だった。「ただし Critical / High に当たるものは、指摘として出したうえでテスト追加も併せて提案する」と、この節の狙いが Medium / Low の漸減であることを両定義に明記した。
- `05:25` - **H2。** `visual-design-reviewer` は承認節が Critical だけを見るため、Critical を閉じた列挙にすると「spec の状態行を満たさない表示」が High 止まりで承認を通る。改訂前はレビュアーの裁量で止められていた穴である。列挙に「spec の完了条件（構造・トークン・状態）を満たしていない表示」を足し、「この定義では High は承認を止めない」ことも併記した。spec 44 行は「**少なくとも**トークン不一致を含む」なので、spec の変更は要らない。
- `05:28` - Medium 3 件・Low 2 件も直した。(M1) visual の根拠欄が「パスと行**または**実測値」で必須項目を省略可能にしていたので併記必須に。(M2) visual のテスト提案規則が「計算スタイル」限定で spec の「決定論チェック」より狭かったので DOM・lint まで広げた（H1 とセットで直さないと抜け穴が広がるので順序を守った）。(M3)「4 項目を欠くものは数えない」と「スキーマに合わない出力は承認として扱わない」が矛盾しうるので「欠いたまま出さない」に寄せた。(L1) P0 の括弧が網羅列挙に読めたので「例:」と明示し認証情報の漏洩を追加。(L2)「すでにテストがある事柄の再指摘はしない」を visual にも置いて対称にした。
- `06:10` - **Verify (外部) 2 回目: 不承認。Critical 0 件・High 1 件・Medium 1 件・Low 1 件。** 前回の H2・M1・M2・M3・L1 は解消と判定された。H1 は「部分的」で、**L2 の対応（対称化）が新しい抜け穴を作っていた**。
- `06:15` - **H1 の残り（自分で作った穴）。** 「すでにテストがある事柄の再指摘はしない」を無条件で節の**最後**に置いたため、直前の但し書き（Critical / High は指摘として出す）を文書順で上書きする読みが成立していた。しかも期待値改ざんはまさに「テストがあり、しかも通っている」状態なので、`Critical の条件` の 1 行目がこの 1 文で消える。両定義の当該文に「Critical / High に当たるもの、および差分が**テストの期待値自体を書き換えている**ときは、テストがあっても必ず指摘する」の例外を入れ、なぜその例外が要るのか（期待値を書き換えれば通る状態はいくらでも作れる）も併記した。
- `06:18` - Medium 1 件・Low 1 件も直した。M1 と M3 の強化が重なり、**スクショしか根拠が無い残差（整列・重なり・階層）が報告できなくなっていた**（承認節 70 行はスクショを根拠として認めているのに、スキーマは「パスと行」を必須にし「欠いたまま出さない」と定めたため）。根拠欄に「行を特定できない残差は、対象要素のセレクタと計測値を根拠にする」「構造の欠落は、あるべき場所のパスと行を書く」を足して、承認節と噛み合うようにした。
- `06:20` - 修正後も `npm run ci` は fail 0。完了条件 5〜8 の 6 語は引き続き両ファイルにヒットする。
- `07:20` - **Verify (外部) 3 回目: 不承認。Critical 0 件・High 1 件・Medium 2 件・Low 1 件。** 前回の High-1・Medium-1・Low-1 は解消と判定された。**High はまた前回の修正が露出させたものだった。**
- `07:25` - **H1（2 定義間の食い違い）。** 前回 visual 側に「期待値を書き換えていれば必ず指摘する」を入れたが、visual の承認節は Critical だけを見るのに、severity は閉じた列挙を通る。列挙にあった改ざんは「**抽出物**をテスト通過のために書き換えている」だけで、`tests/` のテスト期待値の書き換えが入っていなかった。結果、トークン表に載らない値の期待値を書き換えて緑にした差分は High 止まりで承認を通る。一方 `codex-reviewer.md` は「期待値改ざん（spec・テスト期待値・トークン表・Figma 抽出物）」と 4 種類すべてを Critical にしており、**同じ行為の扱いが 2 定義で食い違っていた**。列挙を「抽出物・テスト期待値をテスト通過のために書き換えている」に広げ、`codex-reviewer` と同じ扱いにすると明記した。
- `07:27` - Medium 2 件・Low 1 件も直した。(M1) 残差の根拠が「セレクタと計測値で置き換える」書き方で、spec が必須と定めた「パスと行」が残差では欠けていた。「パスと行に**加えて**書く（省かない）」に変えて両立させた。(M2) lint でしか決まらない指摘を提案へ回すよう求めながら、提案先を `npm run test:e2e` のランナーに限定していたため行き先が無かった（`test:e2e` は lint を回さない）。「lint で決まるものは lint 規則として提案する」を足した。(L1) 追加した 1 文の強調記号が入れ子（`**A**B**C**`）で、最も強調したい部分だけが太字から外れていたので直した。
- `07:30` - 修正後も `npm run ci` は fail 0。完了条件 5〜8 の 6 語は引き続き両ファイルにヒットし、`src/`・`tools/`・`.github/` に差分は無い。
- `07:55` - **Verify (外部) 4 回目: 承認。Critical 0 件・High 0 件・Medium 1 件・Low 3 件。** codex 側も P0・P1 ゼロ。完了条件 1〜10 をレビュー側が独立に再実測して充足と判定した（例 3 行の grep、5〜8 の 6 語が両ファイル、失敗時の規則が両ファイル、`npm run ci` 401 pass・0 fail、`origin/main` 基準で `src/`・`tools/`・`.github/` に差分なし）。**「前回の修正が次の穴を開ける」連鎖は今回で止まった**との判定 — 3 回目までは severity 付与の経路が毎回 1 つ欠けていた（報告義務はあるが列挙に無い、など）が、今回「報告義務 → 列挙 → 承認」の 3 点が初めて全部つながった。
- `08:00` - **承認後だが Medium 1 件・Low 3 件も同じ PR で直した**（レビュー側の推奨。いずれも承認ゲートの外側で、1 行ずつの修正）。(M) 「すでにテストがある事柄の再指摘はしない」が「関連するテストがある」だけで適用でき、テストが触れていない入力の Medium / Low 欠陥を提案ごと握り潰しうる。「テストが**その条件そのものを検証している**事柄」に絞り、関連テストがあるだけでは足りないことを明記した。(L1) 改ざんを Critical にする動機の文言が codex 側「完了条件を満たすための」と visual 側「テスト通過のために」で食い違っていたので codex 側に揃えた。(L2) 「整列・重なり・階層の残差は列挙外」が無条件だったため、残差を通すために期待値を書き換えた差分でも残差側だけ読んで High に落とす読みが残っていた。「ただし残差を通すために期待値を書き換えているなら Critical」を明記。(L3) この差分が codex 側にだけ「コマンド出力は丸ごと貼る」を作っていたので、visual 側にも計算スタイル出力とスクショについて同じ規定を置いた。
- `08:02` - 修正後も `npm run ci` は fail 0、完了条件 5〜8 の 6 語は両ファイルにヒット、`origin/main` 基準で `src/`・`tools/`・`.github/` に差分なし。
- `08:05` - PR #57 を作成し、**今回は作成直後に URL を書いた**（0032・0027・0029・0034 で 4 回続けて忘れていた癖の是正）。ラベルは不要（`.claude/agents/` は保護パス一覧の外）。
